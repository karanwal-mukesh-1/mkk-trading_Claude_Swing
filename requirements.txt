# MKK Institutional Trading System — Python dependencies
# Tested with Python 3.11 on GitHub Actions ubuntu-latest

# Market data
yfinance>=0.2.36

# Data processing
pandas>=2.1.0
numpy>=1.26.0

# DB (sqlite3 is stdlib; sqlalchemy used for pd.read_sql_query compatibility)
SQLAlchemy>=2.0.0

# Formatting / output
tabulate>=0.9.0

# Email (stdlib smtplib is used — no extra dep needed)
# Logging (stdlib — no extra dep needed)
